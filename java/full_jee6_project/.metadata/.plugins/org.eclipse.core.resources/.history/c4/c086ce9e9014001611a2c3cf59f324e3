package br.gov.ce.sefaz.project.manager.impl;

import javax.ejb.Stateless;
import javax.ejb.Timeout;
import javax.persistence.TypedQuery;

import br.gov.ce.sefaz.project.entities.BandeiraPosto;
import br.gov.ce.sefaz.project.manager.BandeiraPostoManager;

@Stateless
public class BandeiraPostoManagerBean extends GenericoManagerBean<BandeiraPosto> implements BandeiraPostoManager {

	private static final long serialVersionUID = 1L;

	@Override
	public BandeiraPosto buscarBandeiraPostoPorDescricaoBandeira(String descricao) {
		try {
			TypedQuery<BandeiraPosto> query = getEntityManager().createQuery("SELECT b FROM BandeiraPosto b WHERE b.nomeBandeira =:descricao", BandeiraPosto.class);
			query.setParameter("descricao", descricao);
			return query.getSingleResult();

		} catch (Exception e) {
			return null;
		}
	}

}
